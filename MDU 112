using UnityEngine;

public class PlayerCore {
    float MaximumHealth = 1000.00f;
    float CurrentHealth = 1000.00f;
    float CurrentPitch=ClampValue(Vertimp,-1,1) ;
    float CurrentYaw=ClampValue(Horzimp, -1,1);
    float SpeedFactor=0f;  
    int Points=0;

    public float LerpValue(float from, float to, float percent)
    {
        return from + percent * (from - to);
    }

    public float ClampValue(float minSpeed, float maxspeed);

    public float UpdatePlayer(){
    float CurrentPitch=ClampValue(Vertimp,-1,1) ;
    float CurrentYaw=ClampValue(Horzimp,-1,1) ;
    int Points=0;
  
    }

    public float GetCurrentSpeed(float minSpeed, float maxSpeed) {

    }

    public void HitObstacle(float impact)
    {
     float CurrentHealth= speedFactor-MaxiumHealth;
     float impact = minSpeed;
          
    }

    public void OnHitHealPickup()
    {
    }

    public void OnHitSpeedBoostPickup() {
    }
}

